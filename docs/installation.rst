============
Installation
============

At the command line::

    $ pip install coast_search

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv coast_search
    $ pip install coast_search
